node_modules
dist
dist-protected/
*.js.map
*.d.ts.map
*.tsbuildinfo
.env
.env.local
.env.*.local
logs/

# Исходники являются источником истины и должны проходить CI/сборку образа.
# Защищённый runtime-артефакт собирается через build:protected.

# Декларации типов в dist-protected — раскрывают структуру кода
*.d.ts
*.d.ts.map
coverage/
