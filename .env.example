# ── Bublik Bot ─────────────────────────────────
# Discord
DISCORD_TOKEN=your_discord_token_here
DISCORD_CLIENT_ID=your_client_id_here

# Если указать — команды (кроме глобальных) регистрируются только для этой гильдии
# Для продакшена оставить пустым — тогда guild-команды пушатся при guildCreate
DEV_GUILD_ID=

# ── Database (PostgreSQL) ─────────────────────
DATABASE_URL=postgresql://bublik:bublik_secret@postgres:5432/bublik?schema=public

# ── Redis ─────────────────────────────────────
REDIS_URL=redis://redis:6379

# ── Bot Settings ──────────────────────────────
DEFAULT_LOCALE=ru
BOT_COLOR=#5865F2
LOG_LEVEL=info

# ── Environment ───────────────────────────────
NODE_ENV=production

# ── PostgreSQL (Docker Compose) ──────────────
POSTGRES_USER=bublik
POSTGRES_PASSWORD=change_me_to_a_strong_password
POSTGRES_DB=bublik

# ── Welcome Module (каналы) ───────────────
# Канал куда отправляется приветствх…
WELCOME_CHANNEL_ID=
# Канал тикетов
TICKET_CHANNEL_ID=# Роль новобранца/кандидата (открывает доступ к каналу тикетов)
RECRUIT_ROLE_ID=